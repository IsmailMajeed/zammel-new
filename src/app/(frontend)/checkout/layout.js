export const metadata = {
  title: "Checkout",
  description: "Secure checkout with open-parcel delivery. Review your order and place it.",
};

export default function CheckoutLayout({ children }) {
  return children;
}


